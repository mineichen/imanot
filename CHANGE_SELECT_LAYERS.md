RectTool select_layers currenttly accepts a iterator of (usize, SortedRanges<u32>). This is not suitable for a public interface, since pixels without corresponding ranges in MaskImage are accepted... Change the interface, to instead accept `AffectedLayer` and gather SortedRanges from MaskImage. Use it in tests, but rewrite tests which use it to partially select Ranges to use select_rect instead. select_layers also didn't calculate the background, which was bad. For the new interface, Background should be None, since select_layers is selecting all pixels from a layer (tell me, if this doesn't make sense to you, otherwise proceed without asking.). If you have to duplicate code, check, if it makes sense to use common helpers, but don't exaggerate. Afterwards, check, if code is only testing a submodule and if so, move the test into the submodule(i expect that there are tests only testing `active_selection/logic` behaviour). Maybe, you can delete some `*_for_test` functions or other `cfg[test]`-only functions.
